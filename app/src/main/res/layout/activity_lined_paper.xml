<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/paper_background">

    <include
        android:id="@+id/viewToobar"
        layout="@layout/toolbar"></include>

    <EditText
        android:id="@+id/title"
        android:layout_width
            ="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/viewToobar"
        android:layout_centerHorizontal="true"
        android:hint="Title"
        android:textSize="22dp" />

    <com.sqisland.android.advanced_textview.widget.LinedEditText xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/title"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="16dp"
        android:background="@color/paper_background"
        android:textColor="@android:color/black"
        android:textSize="22dp" />
</RelativeLayout>